Write a Python program to visualize the Perceptron Learning Algorithm using NumPy and Matplotlib/Seaborn, and evaluate the performance of a single perceptron on the NAND and XOR truth tables as input datasets. During each iteration of training, print the iteration-wise table showing inputs, target output, predicted output, error, and updated weights, and plot the decision boundary defined by  w^Tx=0. Illustrate the relationship between the weight vector (w) and input vectors (x), and use a time delay (sleep logic) between iterations so that the changes in weights and the decision boundary can be clearly observed in the graph.